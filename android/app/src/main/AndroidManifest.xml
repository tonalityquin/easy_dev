<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.quintus.dev">

    <!-- 네트워크 통신(HTTP/HTTPS) 사용 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 네트워크 연결 상태 확인 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 포그라운드 서비스 기본 권한(API 28+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- (API 34+) 포그라운드 서비스 유형: 데이터 동기화 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- (API 34+) 포그라운드 서비스 유형: 미디어 재생(TTS) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- 배터리 최적화 제외 요청(사용자 승인 필요) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 기기 깨우기/유지 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 부팅 완료 브로드캐스트 수신 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Android 13+ 알림 권한 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ✅ 카메라 권한(번호판 촬영/OCR) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ✅ 위치 권한 (앱 사용 중) -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- 카메라 하드웨어 기능 선언(필수 단말만 설치 가능하게 할 경우 required=true) -->
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />

    <application
        android:name="${applicationName}"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Easy Valet(Beta)"
        android:theme="@style/LaunchTheme">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 플러그인 포그라운드 서비스 선언: 데이터 동기화 + TTS 오디오 재생 -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync|mediaPlayback" />
        <!-- 위치를 서비스에서 실제로 사용할 때만:
             android:foregroundServiceType="dataSync|mediaPlayback|location"
             + 매니페스트 상단에 FOREGROUND_SERVICE_LOCATION 권한 추가 필요 -->

        <!-- Flutter 엔진 등록 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ✅ flutter_local_notifications: 예약/부팅 리시버 -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />

        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

    <!-- 외부 앱/인텐트 조회 허용 (필요 대상만 제한적으로 명시) -->
    <queries>
       <!-- 다른 앱의 텍스트 처리(PROCESS_TEXT) 인텐트 조회 허용 -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>
